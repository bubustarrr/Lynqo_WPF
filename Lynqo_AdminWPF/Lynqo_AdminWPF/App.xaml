<Application x:Class="Lynqo_AdminWPF.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:Lynqo_AdminWPF.Converters"
             StartupUri="Views/LoginWindow.xaml">
    <Application.Resources>
        <!-- Lynqo gradient háttér -->
        <LinearGradientBrush x:Key="LynqoGradient" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#FFFC9EFF" Offset="0" />
            <GradientStop Color="#FFFB76FF" Offset="0.4" />
            <GradientStop Color="#FF9D7BFF" Offset="0.7" />
            <GradientStop Color="#FF6A7CFF" Offset="1" />
        </LinearGradientBrush>

        <!-- Kártya stílus -->
        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="CornerRadius" Value="20" />
            <Setter Property="Background" Value="White" />
            <Setter Property="Padding" Value="20" />
            <Setter Property="Margin" Value="20" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#55000000" BlurRadius="20" ShadowDepth="0" />
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Elsődleges gomb -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#FF9D7BFF" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="18" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Placeholder konverter -->
        <converters:EmptyStringToVisibilityConverter x:Key="EmptyStringToVisibilityConverter"/>
    </Application.Resources>
</Application>
