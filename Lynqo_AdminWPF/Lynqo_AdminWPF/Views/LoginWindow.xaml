<Window x:Class="Lynqo_AdminWPF.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="450" Width="400" Background="#120D26" WindowStartupLocation="CenterScreen">
    <Grid Margin="30">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="LYNQO ADMIN" Foreground="White" FontSize="28" HorizontalAlignment="Center" Margin="0,0,0,30"/>
            <TextBox Text="{Binding Username}" Margin="0,0,0,10" Height="35" VerticalContentAlignment="Center"/>
            <PasswordBox x:Name="PassBox" 
             PasswordChanged="PassBox_PasswordChanged" 
             Margin="0,0,0,20" 
             Height="35" 
             VerticalContentAlignment="Center"/>

            <Button Content="BEJELENTKEZÉS" 
                    Command="{Binding LoginCommand}" 
                    Height="45"
                    Style="{DynamicResource LynqoButton}"/>
        </StackPanel>
    </Grid>
</Window>