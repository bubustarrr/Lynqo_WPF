<Page x:Class="Lynqo_AdminWPF.Views.UsersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="UsersPage">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Felhasználók kezelése" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Név" Binding="{Binding Username}" Width="*"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                    <DataGridCheckBoxColumn Header="Kitiltva" Binding="{Binding IsBanned}" Width="70"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <TextBlock Text="Műveletek" FontWeight="Bold" Margin="0,0,0,10"/>
                <Button Content="Kitiltás" Command="{Binding BanCommand}" Margin="0,5"/>
                <Button Content="Feloldás" Command="{Binding UnbanCommand}" Margin="0,5"/>
                <Button Content="Profilkép módosítás" Command="{Binding ChangePictureCommand}" Margin="0,5"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>